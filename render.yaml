services:
  - type: web
    name: fast-video-downloader
    env: node
    plan: free
    buildCommand: npm install && pip install yt-dlp
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
